import { beforeEach, afterEach, beforeAll, afterAll } from 'vitest'

beforeEach(async () => {
  // 清理测试环境
})

afterEach(async () => {
  // 清理测试数据
})

beforeAll(async () => {
  // 全局测试设置
})

afterAll(async () => {
  // 全局清理
})